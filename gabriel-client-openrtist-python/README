INTRO:
This is a python client for 'openrtist_python/proxy.py' proxy server. 


Python client streams camera feed to the backend. openrtist_python transforms the origin image into a stylized image, and then transmit the image (in json) back to the client.


USAGE:
gui version:
./ui.py

command line version:
./client.py

Dependency:
opencv: sudo apt-get install libopencv-dev python-opencv

For gui version, you'll also need pyQt:
sudo apt-get install python-qt4
